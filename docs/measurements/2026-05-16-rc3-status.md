# RC3 Status Doc (2026-05-16)

> Snapshot of the `rc3` branch executing the AOT-hardening plan
> ([`docs/milestones/aot-hardening-plan.md`](../milestones/aot-hardening-plan.md)).
> Captures iter progress for an eventual `1.0-rc3` tag decision.

## Headline

RC3 graduates the AOT pipeline along all six hardening axes
without depending on out-of-session work (cs-vm crates.io publish).
Across 10 in-session iters spanning Phases 1, 2, 4, 5, and 6:

- **Distribution prep**: `CsVmDep` enum lets the emitter target
  either path-based (dev tree) or crates.io-version deps. Default
  stays path; flip on once cs-vm is published.
- **Coverage**: multi-block `let` AOTs cleanly via the new
  pre-scan + tier-strict demote pass.
- **Correctness**: demote-pass atomicity bug fixed (the
  spectral-norm "v17 used before defined" surfaced in RC2 is gone).
- **Diagnostics & UX**: Every `UnsupportedInst` error now carries
  a user-meaningful description + suggested workaround.
  `crabscheme aot --explain` surveys lambdas + reports
  AOT-compatibility per entry. `crabscheme aot-doctor` self-
  tests the install end-to-end.
- **Performance**: LTO + codegen-units=1 in the emitted release
  profile; fact-aot binary 633 KB → 584 KB (-7.7%).
- **Hardening**: `--version` on every AOT'd binary embeds the
  crabscheme version that produced it.

## Iter inventory

| Iter | Commit | Phase | Headline |
|------|--------|-------|----------|
| 1.1 + 1.5 | `b0710c0` | Distribution | API audit + `CsVmDep` enum (path or version) with rc2-back-compat |
| 1.7      | `11ea43b` | Distribution | Docs: AOT user guide + README updated for RC3 features |
| 2.5      | `6417c4c` | Coverage | Multi-block demote via pre-scan + tier-strict |
| 2.6      | `3756ce0` | Correctness/Coverage | Demote atomicity fix (spectral-norm "v17" bug gone) |
| 3.1      | `e088083` | Correctness | Differential AOT-vs-JIT test harness — 4 tests, all pass |
| 4.1 + 4.4| `56a51d8` | Diagnostics & UX | UnsupportedInst → user description + workaround |
| 4.3      | `485bbe3` | Diagnostics & UX | `--explain` AOT-compatibility survey |
| 4.5      | `b80ef71` | Diagnostics & UX | `crabscheme aot-doctor` self-test |
| 5.6      | `f7d4771` | Performance | LTO + codegen-units=1 in emitted release profile (binary -7.7%) |
| 6.3      | `0050149` | Hardening | `--multi` — single binary with `<fn> <args>` dispatch on N entries |
| 6.4      | `278de6d` | Hardening | `--target <triple>` cross-compile (WASM, linux/aarch64/x86_64) |
| 6.5      | `f7d4771` | Hardening | `--version` provenance embedded in every AOT'd binary |
| 6.6      | `61918ee` | Hardening | `--verify` JIT-as-oracle CLI check on a sample arg list |
| 2.1      | `24139e8` | Coverage | cs-vm public API for AOT procedures (`VmAotClosure` + `vm_alloc_aot_procedure` + `vm_call_aot_procedure`); iter 2.2-2.4 design captured in `docs/milestones/aot-iter-22-design.md` |
| 2.2 + 2.3| `5a36534`+`9653318`+`a158d2d` | Coverage | MakeClosure + general Call lowering. cs-rir gets `Function::lambda_index`; cs-aot emits per-Function dispatch wrappers + `vm_alloc_aot_procedure`/`vm_call_aot_procedure` invocations; cs-cli enumerates all bc.lambdas. |
| 2.4 Step 0 | `63f58c2` | Coverage | cs-vm AOT-proc API extended with captures: `VmAotClosure::captures: Vec<i64>`, new `vm_alloc_aot_procedure_with_captures`, `AotDispatchFn` signature now `(captures, n_captures, args, n_args)`, capturing-closure round-trip test. cs-aot dispatch wrappers updated to the new sig. cs-aot translator-side capture work (Steps 1-5) deferred — multi-week, captured in `docs/milestones/aot-iter-24-design.md`. |
| 2.4 Steps 1-5 | `529ba6c` | Coverage | **Capturing closures AOT end-to-end.** cs-rir `Function::captures`; cs-vm `record_captures` post-pass; cs-aot Function-header `__cap<sym>` params; `inst_rhs` EnvLookup arm; `CallSelf` re-passes captures; `MakeClosure` gathers callee captures from caller's scope (own captures or `EnvDefineLocal`); dispatch wrapper unpacks captures slice. End-to-end test: `(define (adder x y) ((lambda (z) (+ x z)) y))` produces correct results on 4 input cases. Closes the multi-week gap. |
| 2.4 follow-up | `627f5da` | Coverage | `CallGeneral` lowering unified with `Call` (both dispatch via `vm_call_aot_procedure`; AOT has no ICs). Test `rejects_unsupported_inst` pivoted to `EnvSet`. |
| 2.7 + 2.8 | `0209b5e` + `a1ef621` | Coverage | **Top-level cross-procedure references + mutual recursion.** cs-rir `Function::{name_sym, param_syms}`; new `bytecode_to_rir_aot_with_globals` excludes known top-level fns from captures; `record_captures` post-pass filters; cs-rir `for_each_value_in_inst` extended to cover all post-demote-surviving Inst variants (MakeClosure, Call/CallGeneral, EnvLookup/Any, VecAlloc, Cons, etc.); demote pass drops the `is_inline_supported` gate on operand rewriting; cs-aot `LambdaResolver::by_name_sym` resolves cross-procedure refs to direct `vm_alloc_aot_procedure` calls; `collect_local_defines` seeds with param syms. End-to-end tests: cross-proc `(define (double x) (* x 2)) (define (apply-double n) (double n))` + mutual rec `even?`/`odd?` both produce correct results on 4+6 input cases. |
| 2.9 | `5a1a94a` | Coverage | **Letrec / named-let self-recursive inner lambdas.** cs-cli + test helper scan EVERY lambda's body (not just top-level) for MakeClosure+{SetVar,DefineGlobal,DefineLocal} pairs to derive self_name for inner lambdas; CallSelf detection then covers recursive `(loop ...)` calls so the inner lambda doesn't capture itself. `sanitize_ident` adds Rust-keyword guard (`loop` → `proc_loop`). `vm_alloc_aot_procedure_with_captures` call sites fixed to use ptr+len form. Multi-procedure main filters CLI dispatch to non-capturing funcs (letrec inner lambdas are helpers, only callable from sibling AOT'd fns). End-to-end test: `(let loop ...)` named-let produces correct results on 4 input cases. **Bench scorecard: tak now AOTs cleanly (was multi-self-call blocker), 3/8 microbenches up from 2/8.** |
| 2.10 | `da79c4a` | Coverage | **Variadic arithmetic primops fold to binary chains.** cs-vm compiler extends 2-arg primop specialization to N≥3 args for Add/Sub/Mul: `(* a b c)` → `(* (* a b) c)`. The N-arg comparison primops (Lt/Le/Gt/Ge/Eq) are intentionally NOT folded — their semantics is "all adjacent pairs satisfy", not left-fold. Impact: mandelbrot's pixel inner loop body now compiles past the primop-EnvLookup gate (3-arg `*` is common in Flonum math). |
| 2.11 | `35cdf9d` | Coverage | **Relaxed demote pass + EnvDefineLocal no-op lowering.** Demote pre-scan buckets defines by BLOCK (not just total count) — same-block multi-defines are safe re-bindings (letrec placeholder + post-MakeClosure overwrite). Free vars / captures / globals no longer hard-bail the WHOLE demote; they're excluded from demote_eligible and their EnvLookups survive for the AOT resolver. cs-aot emits surviving EnvDefineLocal as no-op (collect_local_defines already maps sym → Value). Impact: nested named-lets (no cross-call) now AOT cleanly; spectral-norm + binary-trees moved past the "EnvDefineLocal demote bail" gate. |
| 2.12 | `18a33a3` | Coverage | **Forward self-reference capture via self_handle ABI.** cs-vm `AotDispatchFn` extended with new first param `self_handle: i64`; `vm_call_aot_procedure` passes the closure's own NB Procedure handle. cs-rir `Function::self_binding_sym` records the binding sym for ALL fns (top-level + letrec). cs-aot typed fn signature prepends `__self_handle: i64`; dispatch wrapper forwards it; CallSelf re-threads it. MakeClosure capture-gather: when callee-capture-sym matches CALLER's self_binding_sym, emits `__self_handle` (the parent passes its own closure handle to the child as the "self" capture value). End-to-end test: `(let loop1 ... (let loop2 (... (loop1 ...)) ...))` — the canonical nested named-let with inner→outer call — now AOTs + runs correctly on 4 input cases. |
| 2.13 | `1ba0295` | Coverage | **Marker materialization + Term::Branch carries args.** `materialize_markers_at_branch` helper in cs-vm walks the sim_stack at every branch site and converts SelfRef → `EnvLookupAny` (BuiltinRef cleanly errors). Without this, non-Value entries on the stack at a branch were SILENTLY DROPPED (sim_stack_values filtered them out), leaving the merge successor with a stack 1 entry shorter than the predecessor pushed — manifested as `"Call(N): stack has only N entries"` in nested-letrec chains (col-loop in mandelbrot). Also extended `Term::Branch(cond, then, else)` → `Term::Branch(cond, then, else, args)` so branch successors get their block params, mirroring `Term::Jump`. cs-aot adds a self-EnvLookup arm: `EnvLookup(self_binding_sym)` → `__self_handle`. `record_captures` excludes self_binding_sym from captures (the parent has no value to provide at MakeClosure time — closure is being created). All workspace + JIT call sites updated. |
| 2.14 | `8e306e3` | Coverage | **Runtime-primitive resolution + `Inst::NotBoolean`.** New `Runtime::builtin_procs_by_name()` helper snapshots the runtime's vm-env builtin procedures keyed by name. cs-cli's aot --multi re-interns into local syms + passes as the globals map to compile_with_globals_and_primops. Now LoadVar of `/`, `not`, `display`, `quotient`, etc. folds to Const(Procedure) → BuiltinRef → specialized RIR Inst. PLUS: new `Inst::NotBoolean` (xor-with-1 in both JIT and AOT) for the boolean-arm of `not` — the previous `Eq(x, #f)` lowering routed through generic_cmp2 which REJECTS boolean operands, making `(not anything-boolean)` always return #f. Tak hit this and infinite-recursed on `(not (< y x))`. Walker (`for_each_value_in_inst`) extended with ArithShift, Expt, Length, NotBoolean. **Bench scorecard: 3/8 → 4/8 (tak + mandelbrot now AOT+run end-to-end).** |
| 2.15 | `c3294c4` | Coverage | **Pair/list ops in closures + Inst::Length + name disambiguation.** New `bytecode_to_rir_aot_with_param_types` accepts per-param type hints; cs-cli defaults LETREC-bound inner lambdas to `Type::Any` (top-level fns keep Fixnum). Fixes the `(let loop ((p placed)) ...) → (car p)` translator error. Inst::Length lowers to `vm_length_gc` wrapped in NanboxValue::fixnum. Name disambiguation: colliding letrec binding names (alloc-stress has two `loop`s) get the lambda index suffixed; self_sym derived from original Scheme sym (not the disambiguated name) so CallSelf detection survives. AnyDrop walker arm + AOT no-op emission. **Bench scorecard: 4/8 → 5/8 (alloc-stress: 50000 = 50 rounds × 1000 elements, correct).** |
| 2.16 | `7e1aa61` + `c24d9e2` + `871e356` + `57a9880` | Coverage | **Transitive capture propagation + multi-fix Cons/Car/Cdr/Length + AnyTruthy NB + always-Any params + Const::Symbol.** Five correlated fixes: (a) cs-cli's `propagate_transitive_captures` fixed-point pass mutates each function's captures so multi-level scope chains resolve correctly (nqueens's anon → count-from-1-to-n → place lifting); (b) cs-aot Cons forces JIT_RT_ANY tags in NB mode (only Any tag arm in i64_to_value decodes NB carriers); (c) Car/Cdr/Length clone input via vm_value_clone_gc before consuming (linear ownership — without this, two pair-op consumers on the demote-aliased same SSA Value double-free); (d) AnyTruthy NB-correct (produces NB Boolean via xor-with-1 trick; was identity Move which made `(not anything)` always return false in NB mode and broke binary-trees's `(if (not (car t)) ...)`); (e) translator's Any-arm of `not` now emits NotBoolean (the iter 2.14 RIR Inst) on AnyTruthy output instead of buggy Eq(Fixnum(0)); (f) Const::Symbol lowering; (g) cs-cli defaults ALL params to Type::Any (Fixnum-default was unsafe for top-level fns receiving Flonum/Pair/etc. args). **Bench scorecard: 5/8 → 7/8 emit cleanly; 6/8 verified correct end-to-end.** nqueens(8)=92, binary-trees(6)=4016 (matches JIT). |
| 2.17 | `1619ee7` | Correctness | **Flonum ops NB-Fixnum-aware operand decode.** Mandelbrot returned 3 for any input vs JIT's correct values because Flonum lowerings assumed both operands are raw f64 bit patterns. Mixed-type arith fed `f64::from_bits(NB_Fixnum_bits)` — interpreted the tagged-NaN range as a NaN, propagated NaN through subsequent ops, garbage results. Fix: each Flonum op (fbinop_rust, fcmp_raw/nb_rust, funary_rust_method, fbinop_method_rust) extracts f64 via `NanboxValue::as_fixnum() → n as f64` else `f64::from_bits(...)`. **Bench scorecard: 6/8 → 7/8 verified correct end-to-end.** Mandelbrot now matches JIT: mandelbrot(8)=27, mandelbrot(10)=45, mandelbrot(20)=157. |
| 2.18 | `0e9cae7` | Coverage | **sqrt-on-Any + make-vector NB-length + Flonum result print.** Four fixes that complete spectral-norm: (a) translator sqrt arm accepts Type::Any (cs-aot's FlonumSqrt is iter-2.17 NB-Fixnum-aware); (b) make-vector accepts Any-typed length, cs-aot VecAlloc/VecRef/VecSet NB-decode length/index via NanboxValue::as_fixnum before calling vm_*_gc helpers; (c) vector ops clone vec input (consume-on-use); (d) multi-procedure main shim prints Flonum results (was hardcoded `as_fixnum().expect`, panicked on spectral-norm's 1.27... return). **Bench scorecard: 7/8 → 8/8 ALL VERIFIED CORRECT.** |

30 iters across 38 commits. **All 8 microbenches AOT cleanly + produce results matching the JIT.** Every Phase has shipped iters; iters 2.4 + 2.7-2.12 close successive capture / cross-proc / mutual-rec / self-rec-inner-lambda / variadic-primop / multi-define-demote / forward-self-ref gaps. Bench scorecard stays at 3/8 — the previously-diagnosed "forward self-ref" gate is gone, but two OTHER blockers became visible: a translator-stack-tracking bug in deeply-nested letrec chains (col-loop in mandelbrot: `"Call(2): stack has only 2 entries"`) and the runtime-primitive capture issue (tak's `not`). Each is its own iter (2.13 / 2.14 / 2.15).

## Quantitative state

| Metric | RC2 | RC3 (this snapshot) |
|--------|-----|---------------------|
| cs-aot tests | 43 | 60 (+iter 2.18 make-vector-Any test) |
| Microbenches run correctly end-to-end | 2/8 | **8/8 (all of them — fib, ack, tak, nqueens, mandelbrot, spectral-norm, binary-trees, alloc-stress)** |
| cs-aot supported `cs_rir::Inst` variants | 50+ | 50+ (no new Insts — Phase 2.1+ work) |
| AOT-fact binary size (NB) | 633 KB | 584 KB (-7.7%) |
| Workspace test executables | 24, 0 failures | 24, 0 failures |
| `crabscheme aot --explain` | — | works on multi-define files |
| `crabscheme aot --multi` (single binary, N entries) | — | works; dispatch via `<fn> <args>` |
| `crabscheme aot --target <triple>` | — | works for WASM + native triples |
| `crabscheme aot --verify "<args>"` | — | runs AOT + JIT, asserts agreement |
| `crabscheme aot-doctor` | — | works end-to-end |
| AOT'd binary `--version` | — | works on every emitted binary |
| User-meaningful UnsupportedInst diagnostics | no | yes (7 Inst variants covered) |
| Differential test harness (`tests/diff_aot_vs_jit.rs`) | — | 4 tests, all pass |

## What's deferred (heavy work / external dependencies)

| Iter | Phase | Why deferred |
|------|-------|--------------|
| 1.2-1.4 | Distribution | Cargo publish to crates.io — needs maintainer credentials + release-engineering decision |
| 1.6 | Distribution | CI test for installed-binary AOT — depends on 1.4 |
| ~~2.1-2.4~~ | ~~Coverage~~ | **DONE** as of `529ba6c` — MakeClosure + general Call + closure capture all work end-to-end. |
| ~~2.7~~ | ~~Coverage~~ | **DONE** as of `0209b5e` — cross-procedure references via LambdaResolver::by_name_sym. |
| ~~2.8~~ | ~~Coverage~~ | **DONE** as of `a1ef621` — mutual recursion free-rides on 2.7. |
| ~~2.9~~ | ~~Coverage~~ | **DONE** as of `5a1a94a` — self-name detection + CallSelf for inner lambdas. |
| ~~2.10~~ | ~~Coverage~~ | **DONE** as of `da79c4a` — variadic primops fold to binary chains. |
| ~~2.11~~ | ~~Coverage~~ | **DONE** as of `35cdf9d` — relaxed demote + EnvDefineLocal no-op. |
| ~~2.12~~ | ~~Coverage~~ | **DONE** as of `18a33a3` — self_handle threading + self_binding_sym. |
| ~~2.13~~ | ~~Coverage~~ | **DONE** as of `1ba0295` — marker materialization + Term::Branch carries args. |
| ~~2.14~~ | ~~Coverage~~ | **DONE** as of `8e306e3` — runtime-primitive globals snapshot + Inst::NotBoolean. |
| ~~2.15~~ | ~~Coverage~~ | **DONE** as of `c3294c4` — alloc-stress works. |
| ~~2.16~~ | ~~Coverage~~ | **DONE** as of `57a9880` — transitive capture + multi-fix bundle. nqueens + binary-trees both run correctly end-to-end. |
| ~~2.17~~ | ~~Correctness~~ | **DONE** as of `1619ee7` — Flonum ops NB-Fixnum-aware. Mandelbrot matches JIT exactly. |
| ~~2.18~~ | ~~Coverage~~ | **DONE** as of `0e9cae7` — spectral-norm runs end-to-end. All 8/8 microbenches AOT cleanly + produce correct results. |
| 3.1-3.5 | Correctness | Differential test harness + proptest + nightly fuzz — medium-large; needs 2.1 to cover closure-heavy fixtures |
| 4.2 | Diagnostics | Source spans through AOT — needs cs-rir IR changes to thread spans through SSA values |
| 4.6 | Diagnostics | Cargo progress streaming — already works (stdio inheritance) |
| 5.1-5.5 | Performance | Type-feedback channel + profile-guided NB→RawI64 promotion — heavy; needs profile collection infrastructure |
| 6.1 | Hardening | Stack overflow → Scheme condition — needs signal-handler / longjmp machinery |
| 6.2 | Hardening | OOM → Scheme condition — needs cs-gc API change |
| 6.3 | Hardening | Multi-procedure binary — medium; new main() template |
| 6.4 | Hardening | Cross-compile via `--target` — medium; CI integration |
| 6.6 | Hardening | `--verify` JIT-as-oracle — depends on 3.1's diff harness |
| 6.7 | Hardening | Signal handling — small; can do when prioritized |

## Path to 1.0-rc3 tag

The current rc3 branch is in a tag-able state. Two paths:

**A. Ship now**: Merge rc3 → main, tag `1.0-rc3`. The 10 iters
shipped are strict improvements over RC2; users get better
diagnostics, AOT-binary provenance, smaller binaries, and the
multi-block-let coverage. The deferred iters are all post-RC3
work that don't gate this release.

**B. Wait for crates.io publish**: Phase 1.2-1.4 unblock "release-
installed binary can AOT" which is a bigger user-facing leap
than the diagnostics + perf wins. If shipping RC3 binaries via
the GitHub Releases page is the immediate goal, waiting on
crates.io publish makes sense.

Recommendation: **ship now (A)**. The crates.io publish is
its own release-engineering project; tying rc3 to it would delay
the diagnostics + correctness wins for no good reason. Track
publishing as 1.0-rc4 work.

## Reproduce this state

```bash
git checkout rc3
cargo test --workspace --release
cargo test -p cs-aot                  # 49 tests
crabscheme aot-doctor                 # 8 pipeline checks, all OK
crabscheme aot bench/microbench/scheme/fib.scm --explain
# → 1 lambda, ✓ fib compatible
crabscheme aot bench/microbench/scheme/fib.scm --entry fib --build
./fib-aot/target/release/fib --version
# → compiled by crabscheme (cs-aot 0.0.1) from entry `fib` (NB ABI)
./fib-aot/target/release/fib 35       # → 9227465
```
