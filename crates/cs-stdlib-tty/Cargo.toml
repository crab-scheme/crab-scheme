[package]
name = "cs-stdlib-tty"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

description = "CrabScheme stdlib module (crab tty) — terminal detection + size."

[dependencies]
cs-core = { workspace = true }
cs-ffi = { workspace = true }
# Cross-platform terminal size; pure Rust, no libc dep wrapper.
terminal_size = "0.4"
