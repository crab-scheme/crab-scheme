[package]
name = "cs-stdlib-ini"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

description = "CrabScheme stdlib module (crab ini) — INI configuration parsing and serialization."

# Pure Rust, no dependencies — portable everywhere including wasm32.
[dependencies]
cs-core = { workspace = true }
cs-ffi = { workspace = true }
