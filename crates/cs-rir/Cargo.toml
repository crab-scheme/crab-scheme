[package]
name = "cs-rir"
version = "0.0.1"
edition = "2021"
publish = false

description = "CrabScheme JIT-backend-agnostic intermediate representation."

[dependencies]
cs-core = { path = "../cs-core" }

[dev-dependencies]
